<doc>
  <head>
    <title>Liquid Site Template API</title>
  </head>

  <body>

    <h1>The document data object</h1>

    <p>The document data object is used to access the content of a
    document.</p>

    <list style="compact">
      <item>
        <title><ref id="get">[property name]</ref></title>
        <text>Returns the data value associated to a specified
        property name.</text>
      </item>
    </list>


    <separator/>

    <h2 id="get" style="inner">[property name]</h2>

    <p>Returns the data value associated with a specified document
    property. Document properties will always be returned in HTML
    format.</p>

    <h3>Syntax:</h3>

    <p style="indent"><code>[property name] &#8594; string</code></p>

    <h3>Returns:</h3>

    <p style="indent">The data value associated to a specified
    property name.</p>

    <h3>Example:</h3>

    <p>The following code obtains the values for two document
    properties:</p>

    <example>
      <preformat>&lt;#assign doc = liquidsite.findDocument("Section1/doc1")&gt;
Document title: ${doc.title}
Document text:  ${doc.text}</preformat>
    </example>

  </body>
</doc>
